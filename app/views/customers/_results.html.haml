.customers-results
  - placeholder_message = capture do
    = icon :warning
    No customers found for
    %strong= search_query

  = collection_placeholder customers, message: placeholder_message do
    - caption = capture do
      = icon 'search'
      = page_entries_info customers
    = render 'table', customers: customers, caption: caption
    = render 'common/pager', collection: customers
