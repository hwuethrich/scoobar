= simple_form_for([event, booking]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :customer, as: :customer, readonly: booking.persisted?

  %p.form-actions
    = f.button :submit

